
# Cities ------------------------------------------------------------------
# which cities are in the data set?
states <- cleaned_business_data |> 
  distinct(state)

cities <- cleaned_business_data |> 
  distinct(city)



















